// import { Link } from "react-router-dom";
import styles from "./homeService.module.css";

export default function Header() {
  return (
    <div className={styles.titleContainer}>
      <h2 className={styles.titleService}>SERVICES PROPOSÉS</h2>
    </div>
  );
}
